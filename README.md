# where-to-watch

Which country do I point my VPN at to stream this movie or show?

Search a title and see, per streaming service, every country where it's included with a subscription — 139 countries in a single query, straight from the browser. No backend.

```sh
npm install
npm run dev
```

Deploys to GitHub Pages automatically on push to `main`.
